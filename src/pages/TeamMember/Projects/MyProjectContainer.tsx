import { Outlet } from "react-router-dom";

const MyProjectContainer = () => {

    return (
        <div className="">
            <Outlet />
        </div>
    );
};

export default MyProjectContainer;
